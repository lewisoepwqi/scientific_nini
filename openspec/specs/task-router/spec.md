# Capability: task-router

## Purpose

_已移除。_ TaskRouter 随 simplify-multi-agent 变更一起废弃。主 Agent LLM 在调用 dispatch_agents 时直接声明 agent_id，不再需要中间路由推理层。详见 `multi-agent-routing` capability 的移除记录。

## Requirements

_所有要求已移除。此 spec 保留为历史记录。_
