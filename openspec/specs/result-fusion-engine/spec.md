# Capability: result-fusion-engine

## Purpose

_已移除。_ ResultFusionEngine 随 simplify-multi-agent 变更一起废弃。子 Agent 原始输出直接以 agent_id 标签拼接返回，主 Agent 在下一轮自行综合，无需独立融合层。详见 `multi-agent-fusion` capability 的移除记录。

## Requirements

_所有要求已移除。此 spec 保留为历史记录。_
