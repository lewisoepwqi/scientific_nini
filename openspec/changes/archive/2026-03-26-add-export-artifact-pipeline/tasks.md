## 1. 导出作业与产物清单

- [x] 1.1 定义项目产物清单与导出作业的数据模型
- [x] 1.2 为正式导出结果接入统一产物登记与版本号分配
- [x] 1.3 实现项目级多产物打包下载接口

## 2. 报告与图表导出

- [x] 2.1 扩展报告会话导出，支持 Word、PPTX 与 LaTeX 模板
- [x] 2.2 扩展图表导出 metadata 与格式状态声明
- [x] 2.3 确保 Evidence Block、METHODS 与图表资源引用在导出中保持一致

## 3. 工作区与验证

- [x] 3.1 在工作区增加项目产物列表与多选下载入口
- [x] 3.2 为导出作业、产物登记和打包下载补充后端测试
- [x] 3.3 运行 `pytest -q` 与 `cd web && npm run build`，并记录旧下载入口与新导出作业的兼容策略

## 兼容策略

- 旧入口 `/api/workspace/{session_id}/download-zip` 保留，继续按工作区路径打包，避免旧版批量下载和脚本调用中断。
- 新入口 `/api/workspace/{session_id}/project-artifacts/download-zip` 只面向正式项目产物，优先在前端产物画廊中使用。
- 报告正式导出统一落在 `artifacts/exports/` 并登记导出作业；普通文档导出仍保留原工作区文档路径语义，避免影响旧文档预览与下载。
