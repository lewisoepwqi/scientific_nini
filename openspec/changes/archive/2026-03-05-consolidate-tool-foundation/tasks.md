## 1. 统一资源类型与执行基座

- [x] 1.1 盘点现有 `SkillResult`、工作区索引、执行历史结构，定义统一 `resource_id` / `resource_type` 类型与序列化结构
- [x] 1.2 新增基础资源模型与运行记录模块，覆盖数据集、统计结果、图表、报告、脚本、工作区文件的公共摘要字段
- [x] 1.3 为会话资源建立持久化目录和索引读写约定，明确脚本、图表、报告与中间产物的落盘位置

## 2. 基础工具层实现

- [x] 2.1 实现 `task_state`，承接当前任务初始化、更新和查询能力
- [x] 2.2 实现 `dataset_catalog`，整合数据集加载、预览、摘要与质量概览能力
- [x] 2.3 实现 `dataset_transform`，支持高频结构化数据清洗、拼接、聚合与步骤级 patch
- [x] 2.4 实现 `stat_test`、`stat_model`、`stat_interpret`，整合现有统计检验、建模与解读工具
- [x] 2.5 实现 `chart_session`、`report_session`、`workspace_session`，建立图表、报告与工作区资源生命周期

## 3. 脚本会话与失败恢复

- [x] 3.1 实现 `code_session` 资源模型，支持 Python/R 脚本创建、读取、执行和历史关联
- [x] 3.2 将现有 `run_code` / `run_r_code` 核心执行能力迁移到脚本会话链路
- [x] 3.3 实现脚本 `patch_script`、`rerun` 与显式输出提升能力，确保数据集和产物能稳定注册为会话资源
- [x] 3.4 为脚本失败结果补充错误定位、恢复提示和与前次执行的关联记录

## 4. 编排层与工具注册切换

- [x] 4.1 将 `complete_comparison`、`complete_anova`、`correlation_analysis`、`regression_analysis` 重构为内部编排层，改为组合基础工具执行
- [x] 4.2 更新工具注册表，仅向模型暴露新的基础工具集合，移除旧 30 工具的正式暴露入口
- [x] 4.3 调整 Agent 运行时与提示词上下文，使继续操作优先使用 `resource_id` 和基础工具，而不是隐式 `latest_*` 状态

## 5. 工作区、报告与图表链路适配

- [x] 5.1 更新工作区索引、执行历史和资源列表接口，支持脚本、图表会话、报告会话等新资源类型
- [x] 5.2 更新图表导出链路，改为从图表会话资源执行导出并登记产物
- [x] 5.3 更新报告生成与导出链路，支持章节级 patch、结构化资源绑定和多格式导出
- [x] 5.4 清理旧的自由路径写入和“最近一次图表/报告”隐式状态代码

## 6. 前端、测试与文档

- [x] 6.1 更新前端工作区、执行历史、报告下载和图表预览相关状态与展示逻辑，识别新工具名和资源类型
- [x] 6.2 为基础工具、脚本会话、编排层和失败恢复补充单元测试与回归测试
- [x] 6.3 使用会话 `13839f39e762` 设计端到端回放测试，验证中间数据集复用、路径受管输出和局部 patch 恢复
- [x] 6.4 更新开发文档、API 文档和技能/工具说明，移除旧 30 工具的正式接口描述
